db = db.getSiblingDB('pokecarddb');
// Use mongoimport or JS loops to load files (but since files are local, mount them and run mongoimport in a script).
// Alternatively, handle import in Go seed command.